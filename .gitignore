# Session-start briefing for Claude Code. Gitignored deliberately: it loads from
# disk regardless of git, and every tracked file is served as public plain text.
CLAUDE.md

documents/
workflow/
plan/
prompts/
.claude/
feedbacks/
seo/

# IDE
.vscode/

# Intermediate image dumps (Midjourney raw, before scripts/optimize-images.py)
assets/images/automotive/automotive_frames_named/

# Premiere working files + source clips (only cover-video.mp4 ships)
assets/cover/Adobe Premiere Pro Auto-Save/
assets/cover/*.prproj
assets/cover/video-*.mp4
.gstack/
assets/images/automotive/automotive_images_final_named/
assets/images/automotive/automotive_images_final_named.zip

# Per-vertical local-only artifacts (co-located with each vertical)
*/images/_masters/
*/prompts*.md
generic/seo/

# credx-website working copy docs — internal. They carry client quotes, open
# items and review notes, and every tracked file is served publicly by both
# GitHub Pages and Vercel. Same treatment as documents/, plan/, seo/, workflow/.
credx-website/copy/

# ── Internal materials. NEVER tracked: every tracked file is served as public
#    plain text by Vercel. These carry client quotes, review commentary, open
#    items and the internal forbidden-language list.
#    Added 2026-08-18 when the four verticals were closed and handed over.
copy/
*/copy.md
*.docx

# Impeccable working context. Internal: client quotes, anti-references, brand
# strategy. Same treatment as copy/ and workflow/ — never served publicly.
PRODUCT.md
DESIGN.md
*/PRODUCT.md
*/DESIGN.md
